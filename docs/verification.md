# 検証状況

| 対象 | 状況 |
|---|---|
| Cookie 抽出・セッション確立 | 実アカウントで確認済み（購入作品を全件取得） |
| ライブラリ取得・メタデータ解析 | 実データ（200 作品ほど）で確認済み |
| ダウンロードプラン解決 | 実作品 10 件で確認済み（すべて direct 判定） |
| 別環境の登録の混入 | 開発機の Windows 側と SteamDeck 側の登録を全件調べ、**どちらも自分の環境のパスだけ**で混入は無かった。ただし Steam はデバイス間で情報を見せることがあるため、パス表記で判別して数えない作りにしてある（実物の写しに Windows 形式のエントリを注入して、除外と操作の遮断を確認済み） |
| `shortcuts.vdf` の読み書き | 実ファイル（既存の登録あり・日本語名含む）で確認済み。バイト単位の round-trip 一致、追加・冪等な再実行・解除まで検証（実ファイルには書き込まず複製で実施） |
| Linux 上での動作 | Python 3.8 / Ubuntu で全テスト通過。Windows 製 ZIP のバックスラッシュ区切り、大小のみ異なるファイル名、Linux 固有の文字を含む名前、実行ビットの保持、Firefox プロファイル検出、Linux パスでの状態保存と Steam 登録を確認済み |
| 実ダウンロードと展開（単一 ZIP） | 実作品 2 件で確認済み。ダウンロード → ZIP 展開 → 単一フォルダの引き上げ → exe 検出 → 状態記録まで通した |
| 実ダウンロードと展開（分割 RAR） | 実作品 1 件（2.76 GB / 3 分割）で確認済み。`part1.exe` + `part2.rar` + `part3.rar` を取得し、7-Zip 経由で 39 ファイル 2.6 GB を展開、`Game.exe` を検出（`Config.exe` ではなく本体を選べている） |
| SteamDeck 実機（SteamOS / Python 3.13.5） | 全テスト通過。`check`・Web UI・SD カード（ext4）へのダウンロードと展開・キャッシュ削除まで確認。Flatpak 版 Firefox の Cookie 読み取り、セッションの自動張り直し、`/usr/bin/7z` の検出も動作 |
| シリアルコード必須作品 | **未検証**（該当は 1 割弱、最小でも数百 MB） |
| Web UI | 実ライブラリ（200 作品ほど）で確認済み。カテゴリ切替（ゲーム / マンガ / CG / ボイス / 音楽 / 動画）、exe が複数ある作品での選択表示、DLsite タイトルの初期値表示まで確認 |
| Web UI の状態・設定タブ | 実環境で確認済み。状態タブは CLI の `check` と同じ内容（Cookie の読み取り・セッション有効・購入件数・7z 検出・Steam 起動状態）を表示。設定タブは当時の全 18 項目（現在は 23 項目）の表示、条件付き項目の出し分け、保存と `config.json` への反映、不正値 7 種の拒否を確認 |
| インストール先の切り替え | 実環境で確認済み。設定タブで 2 つ目を追加すると既定の選択肢が即座に追従し、ライブラリ画面では既定が選択済みになる。既定でないほうを選んで実際に 1 件ダウンロードし、選んだ側に展開されること、展開後にアーカイブのキャッシュ（37.3 MiB）とその置き場が消えることを確認 |
| Steam への実書き込み（UI 経由） | 実 `shortcuts.vdf`（既存の登録あり）に 2 作品を追加して確認済み。既存の登録は無変更、日本語かつ全角スペースを含む exe パスも正しく記録。同じ作品を再登録しても重複せず、改名した場合は旧名の登録が残らないことも確認。書き込みのたびに `shortcuts.vdf.bak-<日時>` を作成 |
| ライブラリの表紙・背景 | 実 `grid/` に 2 作品ぶん 6 枚を設置して確認済み。AppID の符号なし表現でファイル名が付き、既存の設定済み画像には触れない。ただし DLsite の画像は 560x420 で比率が合わない |
| ロゴ（exe のアイコン） | 実物の exe 3 種で確認済み。256x256 の 24bpp DIB / 32bpp DIB / 32x32 いずれも PNG 化に成功し、色順・行順・透過が正しいことを目視でも確認 |
| Steam を終了する（UI 経由） | SteamDeck 実機の Desktop Mode で確認済み。Steam 起動中は帯が出て Steam 操作がすべて無効になり、「Steam を終了する」で約 23 秒後にプロセスが消え、帯が消えて操作可能に戻った。**`shortcuts.vdf` はバイト数・件数とも変わらず無傷**（`-shutdown` を使う理由がこれ。`pkill` では書き戻し前に死ぬことがある） |
| タイトル入りカバー・ロゴ | SteamDeck 実機で確認済み。実ライブラリから短い・中くらい・長い（日英混在）タイトルの 3 件を実画像で描画し、いずれも 600x900 の PNG になること、日本語が字形として正しく出ること、長いものが 4 行に折り返して自動で縮むことを目視で確認。ロゴは 1 行のまま横に伸び（160px〜1954px）、透過 PNG になる |
| カバーの作り直し | SteamDeck 実機の **`grid/` を丸ごと複製した環境**で確認済み（実ファイルには書き込んでいない）。実 `state.json` の登録済みから 3 件を対象に実行し、複製内の画像が枚数を変えずに差し替わること（余りも欠けも出ない）、他のゲームの画像に触れないことを確認。タイトル埋め込みとロゴを切ると該当の 3 枚が引き上げられ、戻すと元通りになる（片道にならない）。**2 回目以降は通信を一切しない**ことを、通信しようとしたら落ちる差し替えで確認（3 件 1.1 秒） |
| 描ける環境かどうかの判定 | SteamDeck 実機で確認済み。通常は「描ける」と判定して既定 ON。**日本語フォントを外した fontconfig** を用意して同じ判定を走らせると「描けない」となり既定が OFF に変わり、理由（フォントが無い旨）が出ることを確認。なお当初は「豆腐どうしは同じ絵になる」前提で描き比べる実装にしていたが、**実機では符号位置の 16 進を含む別々の絵になり検出できなかった**ため、fontconfig に尋ねる方式に改めた |
| 普通の Linux デスクトップ (Ubuntu 24.04 コンテナ) | 確認済み。SteamOS ではない素の Ubuntu + XFCE で全テストが通り、`check` が**購入件数の取得**まで到達。Firefox 156 (Mozilla の deb) の Cookie を読み、セッション確立まで成功。`rsvg-convert` と Noto CJK が揃うためカバーへのタイトル埋め込みも既定 ON になる。**この検証で、Firefox がプロファイルを `~/.config/mozilla/firefox` へ移していたことが判明し、探索先を追加した** |
| 新品の Steam への Proton 割り当て | 素の Ubuntu に入れた新品の Steam で確認済み。`config.vdf` に `CompatToolMapping` の節が無い状態から、節ごと作って書き込めること、括弧が釣り合うこと (219/219)、既存の設定 (`Accounts` など) が残ること、控えが作られることを実ファイルで確認。**この環境で初めて出た不具合で、実機では既に割り当てがあったため気付けなかった** |
| Steam を終了する（Deck 以外） | Ubuntu 24.04 コンテナで確認済み。`/usr/bin/steam` が無い環境でも PATH から `/usr/games/steam` を引き当て、**ログイン済みの Steam を 4.0 秒で終了**、`shortcuts.vdf` は 425 バイト・1 件のまま無傷。**サインイン画面のままの Steam は 90 秒待っても終了しない**（指示は届くが処理されない）ので、検証するときはログインを済ませておくこと |
| 登録解除とゴミの削除 | 実 `shortcuts.vdf` と `grid/` を丸ごと複製した環境で、CLI (`steam --remove`) と Web UI の「解除」の両方を確認済み。エントリの削除と添字の振り直し、その作品の画像 4 枚の削除、他作品の画像を残すこと、`state.json` の `steam_app_id` が `None` に戻ることまで確認 |

購入済み作品のダウンロード種別を全数調査した結果、大半が direct で、serial が 1 割弱、split は 1 作品のみ。
分割 RAR は現在ほぼ絶滅しており、該当は 1 作品のみだった。
